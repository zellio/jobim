#!/usr/bin/env ruby

require 'pathname'
file = Pathname.new(__FILE__).realpath

$:.unshift File.expand_path("../../lib", file)

require 'jobim'

opts = Jobim::CLI.run!(*ARGV)

exit if opts.nil?

Jobim::Server.start opts
